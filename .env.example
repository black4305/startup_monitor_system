# AI 지원사업 모니터링 시스템 환경 설정 예제
# 이 파일을 복사해서 .env 파일로 만들고, 실제 값을 입력하세요.
# cp .env.example .env

# 🗄️ Supabase 설정 (필수)
# Supabase 프로젝트에서 이 값들을 찾을 수 있습니다
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key-here
SUPABASE_SERVICE_KEY=your-service-role-key-here

# 🔐 보안 키
# 프로덕션 환경에서는 반드시 강력한 시크릿 키를 사용하세요
SECRET_KEY=your-secret-key-here-change-in-production

# 🌐 Flask 설정
FLASK_ENV=development
FLASK_DEBUG=false
FLASK_HOST=0.0.0.0
FLASK_PORT=5001

# 🤖 AI 모델 설정
# AI 모델 파일이 저장된 경로
COLAB_MODEL_PATH=models/

# 크롤링 성능 설정
MAX_CONCURRENT_REQUESTS=10
CRAWL_DELAY_SECONDS=1

# 📊 로그 설정
LOG_LEVEL=INFO
LOG_TO_FILE=false
LOG_RETENTION_DAYS=90
AUTO_CLEANUP_ENABLED=true

# 🐳 Docker 설정 (선택적)
# Docker Hub에 이미지를 푸시하는 경우에만 필요
DOCKER_USERNAME=your-docker-username
DOCKER_PASSWORD=your-docker-password
IMAGE_TAG=latest

# 🔧 성능 튜닝 (선택적)
# 디바이스별 설정 (GPU/CPU)
MAX_CPU_THREADS=4
REQUEST_DELAY=3.0
MIN_SCORE_THRESHOLD=55
MAX_PROGRAMS_PER_SITE=50

# 📧 알림 설정 (선택적)
# 이메일 알림을 사용하는 경우
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
NOTIFICATION_EMAIL=admin@example.com

# 🔍 크롤링 설정
CRAWLER_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
CRAWLER_TIMEOUT=30
CRAWLER_MAX_RETRIES=3

# 💾 캐시 설정
CACHE_DURATION=300  # 5분
ENABLE_CACHE=true

# 🌐 프록시 설정 (선택적)
# 기업 프록시를 사용하는 경우
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NO_PROXY=localhost,127.0.0.1,.company.com